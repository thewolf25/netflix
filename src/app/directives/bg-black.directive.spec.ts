import { BgBlackDirective } from './bg-black.directive';

describe('BgBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new BgBlackDirective();
    expect(directive).toBeTruthy();
  });
});
